# case-github-actions-fix-improve-bookmark — 发布说明

_更新: 2026-08-20_

新增 fix/improve/feature 发布日志的自动同步：`develop` 上 `status: released` 的 release-notes.md 会按 `change_type` 分类，渲染成 case 页面与索引并同步到 `pyff-ny/dev-log`（GitHub Pages）。复用 `markdown_parser.py`，无新增外部依赖；支持一次 push 多文件的幂等同步。
