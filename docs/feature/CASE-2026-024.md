# Goal DORA 概览

_更新: 2026-08-12_

Goal 详情页"活动"热力条下面新增"DORA 概览"：两个数字卡片，Lead Time（中位数，创建到发布的天数）和部署频率（已发布 Case 之间的平均间隔天数）。数据来自各 Case 已有的 `created_at`/`released_at`，不新增后端接口、不改 schema，也不发任何新请求。这是把 DORA（DevOps Research and Assessment）度量维度接入 case-manager 的第一步——只做度量，不改 case-manager 自己的阶段词汇。
