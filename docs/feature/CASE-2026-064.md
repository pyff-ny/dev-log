# mk-hub-serve-registry — 发布说明

_更新: 2026-08-21_

v2.9.0 初版发布内容：

- study-tracker 注册到统一 `capability_registry.tsv`，通过 `serve_entry.py` 使用项目 venv，运行在 `8720`。
- mk_hub 新增「mk serve registry」tab。
- 支持查看、新增、编辑、启用/禁用 serve 注册项；CLI 行只读，禁用代替物理删除。
- registry 写入具备校验、恢复备份和原子替换。
- 保持既有 `mk serve` 生命周期、输出契约和 JSON schema 不变。
